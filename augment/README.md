# Part E – Augment the Study

We selected the “reduction to Independent Set” option for Min Vertex Cover.

This folder includes:

- `vc_to_is.py`: Convert vertex-cover adjacency graph → independent-set edge list.
- `is_to_vc.py`: Convert an independent-set solution back into a vertex cover.
- `test/`: Example graphs and a sample IS solution.

## Example usage

VC → IS:
